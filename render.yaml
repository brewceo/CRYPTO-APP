services:
  - type: web
    name: crypto-app
    env: node
    plan: free
    buildCommand: pnpm install --frozen-lockfile && pnpm run build
    startCommand: pnpm run preview
    envVars:
      - key: NODE_ENV
        value: production
